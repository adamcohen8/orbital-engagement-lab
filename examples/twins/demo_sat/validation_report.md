# Spacecraft Twin Validation: Demo Synthetic Satellite Twin

- Schema: `oel.spacecraft_twin.v0`
- Object ID: `demo_sat`
- Version: `0.1.0`

This checked-in report is a public-safe example artifact. Regenerate it with:

```bash
.venv/bin/python tools/build_spacecraft_twin.py examples/twins/demo_sat/twin.yaml --validate --report --print-summary
```

The demo package uses synthetic geometry, mass properties, evidence, and
assumptions for workflow validation only.
